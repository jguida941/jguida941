# dev/reports/archive

Subject-scoped frozen evidence trees kept for provenance.

Closed evidence home in the org root-shape cover; artifacts are enumerated in the
typed `manifest.json` bijection. This home lands EMPTY at org L1 and is populated at
L2 through the data-driven birth router. `manifest.json` and this `README.md` are the
category control files (excluded from the artifact bijection).
