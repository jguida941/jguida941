# skills/ — packaged agent skills

Governed by `contracts/repo_layout.json` (the ratified root-shape owner) and enforced by
`tests/contracts/test_org_rootwide.py` (the `test_org_rootwide` guard). This README teaches the
current law for the `skills/` family; the guard reddens if it drifts back to retired teaching.

## Current members

- `design-language-tdd/` — the repeatable design-language TDD skill (`SKILL.md` plus its
  `references/`). It teaches the live 13-step slice ritual, the three-verdict gate cadence, the
  review-convergence law, and the binding MF1 Chrome-headless receipts, and it routes to the
  plan-of-record (`docs/plans/ACTIVE.md`) and the SOP (`AGENTS.md`).

Add a skill and declare its home in the same slice; a silent drop reddens the org guard.

## Retired law (do NOT reintroduce)

- The `candidate_only` / `singleton`-enforcement framing is superseded by the ratified
  root-shape-owner metadata; do not teach a skill as an indefinitely-owed candidate.
- The killed lane's `proposals` evidence category is retired and never re-enters.
