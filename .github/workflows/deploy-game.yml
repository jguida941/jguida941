name: Deploy Developer Quest Game

on:
  workflow_dispatch:
  push:
    branches: ["main"]
    paths:
      - "game/**"
      - ".github/workflows/deploy-game.yml"

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: actions/configure-pages@v5
        with:
          token: ${{ secrets.PERSONAL_GITHUB_TOKEN || github.token }}
          enablement: ${{ secrets.PERSONAL_GITHUB_TOKEN != '' && 'true' || 'false' }}

      - uses: actions/upload-pages-artifact@v3
        with:
          path: game

      - id: deployment
        uses: actions/deploy-pages@v4
